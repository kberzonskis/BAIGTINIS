export function PublicPageTitle({ title }) {
    return (
        <div className="container">
            <div className="row">
                <h1 className="col-12  mt-3 mb-5">{title}</h1>
            </div>
        </div>
    );
}